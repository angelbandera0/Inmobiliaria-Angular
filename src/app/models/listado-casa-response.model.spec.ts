import { ListadoCasaResponse } from './listado-casa-response.model';

describe('ListadoCasaResponse', () => {
  it('should create an instance', () => {
    expect(new ListadoCasaResponse()).toBeTruthy();
  });
});
